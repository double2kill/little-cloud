{
  "name": "little-cloud",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @little-cloud/web dev",
    "build": "lerna run build",
    "deploy": "bash scripts/deploy.sh",
    "test": "lerna run test",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "lerna": "^8.1.9"
  },
  "engines": {
    "node": ">=18"
  }
}
